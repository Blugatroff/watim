[== Stdin Compiler ==]
struct Array<T> {}

fn bar<T>(a: T, b: T) { }

fn foo() {
    make Array<i32> make Array<bool> bar<_>
}
[== Args Compiler ==]
check
-
--quiet
[== Status Compiler ==]
1
[== Stderr Compiler ==]
-:6:38 call to `bar` expected the following arguments:
	[T, T]
	T = Array<i32>
but got:
	[Array<i32>, Array<bool>]

[== Stdin Compiler ==]
struct Array<T> {}

fn bar<T>(a: i32, b: Array<T>, b: i32) { }

fn foo() {
    make Array<i32> 0 bar<_>
}
[== Args Compiler ==]
check
-
--quiet
[== Status Compiler ==]
1
[== Stderr Compiler ==]
-:6:23 call to `bar` expected the following arguments:
	[i32, Array<T>, i32]
	T = unknown
but got:
	[Array<i32>, i32]
